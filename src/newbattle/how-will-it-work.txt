Interaction:

Sprites and containers have a .on("click") or .on("pointertap") for receiving clicks

Attack and defense animations:

Sprites/containers can also listen to or emit arbitrary events, such as .on("attacked").

State updates:

Probably also handle that through event emitters.
