import React from "react"
import { Toaster } from "react-hot-toast"
import './App.css'

export default function App(): JSX.Element {
    return <div>
        <Toaster />
        <h2>Hello, furld</h2>
    </div>
}