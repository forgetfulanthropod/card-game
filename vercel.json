{
  "version": 2,
  "framework": null,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm run build",
  "builds": [
    {
      "src": "builds/server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["public/**"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "builds/server.js" }
  ],
  "env": {
    "IS_PRODUCTION": "true",
    "DEV_STATIC_ASSETS": "true",
    "NODE_ENV": "production"
  }
}