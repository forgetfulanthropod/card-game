// Single source of truth for the staged public location used by stage, includeFiles, resolve, and inspector.
export const STAGED_REL = 'api/bundled-public';
export const FUNC_REL = 'api/bundled-public'; // layout inside .func (prefixed by include)
export const BUNDLED_DIR_NAME = 'bundled-public';
