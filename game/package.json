{
    "name": "game",
    "scripts": {
        "build": "rm -rf dist && tsc && esbuild --bundle exports.ts --platform=node --outfile=./dist/exports.js"
    },
    "dependencies": {
        "angu": "^0.12.0",
        "seedrandom": "^3.0.5",
        "shared": "workspace:shared@*"
    },
    "devDependencies": {
        "@types/seedrandom": "^3.0.2"
    }
}
