export { applyDamage } from './util'

export {
    explain as explainAddBlock,
    execute as executeAddBlock,
} from './addBlock'
export { explain as explainChain, execute as executeChain } from './chain'
export { explain as explainDeal, execute as executeDeal } from './deal'
export {
    explain as explainDebilitate,
    execute as executeDebilitate,
} from './debilitate'
export { explain as explainOrb, execute as executeOrb } from './orb'
export { explain as explainText, execute as executeText } from './text'
