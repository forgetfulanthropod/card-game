// Game
{
    "extends": "../tsconfig.base.json",
    "compilerOptions": {
        "paths": {
            // just makes the good import come first in list
            "shared": [
                "../shared"
            ],
            "shared/code": [
                "../shared/code"
            ],
            // "server": [
            //     "../server"
            // ],
            // "server/metrics": [
            //     "../server/metrics"
            // ],
            "@/*": [
                "./*"
            ]
        }
    },
    "references": [
        {
            "path": "../shared"
        },
        // {
        //     "path": "../server"
        // }
    ]
}
