import { rulebook } from '@@/db/rulebook'

export const {
    characters,
    initialScenes,
    items,
    locations,
    moveModifiers,
    recipes,
    numbers,
    stanceTypeMetaMap,
    moveModiferMap,
} = rulebook
