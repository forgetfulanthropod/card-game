import { rulebook } from '@@/db/data'

export const {
    characters,
    initialScenes,
    items,
    locations,
    moveModifiers,
    recipes,
    numbers,
    stanceTypeMetaMap,
    moveModiferMap,
} = rulebook
